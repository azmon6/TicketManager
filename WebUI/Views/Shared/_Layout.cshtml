<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <link href="~/Content/HeadStyle.css" rel="stylesheet" />
    <link href="~/Content/MyStyles.css" rel="stylesheet" />
    <script src="https://kit.fontawesome.com/7bc2b6c846.js" crossorigin="anonymous"></script>
</head>
<body>
    <div class="HGparent">
        <header class="HGheader">
            <div class="page_header">
                <div class="logo">
                    Ticket Manager
                </div>
                @*TODO Dropdown when small screen*@
                <ul>
                    <li>
                        @Html.ActionLink("Home", "HomeScreen", "Home")
                    </li>
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li>
                            @Html.ActionLink("View Cart", "ShowCart", "Cart")
                        </li>
                        <li>
                            @Html.ActionLink("Log Out", "LogOut", "Account")
                        </li>
                    }
                    else
                    {
                        <li>
                            @Html.ActionLink("Login", "Login", "Account")
                        </li>
                        <li>
                            @Html.ActionLink("Sign Up", "SignUp", "Account")
                        </li>
                    }
                </ul>
            </div>
            @if (TempData["error"] != null)
            {
                <div>
                    @TempData["error"]
                </div>
            }
        </header>

        <div class="HGleft">
        </div>

        <div class="HGmain">
            @RenderBody()
        </div>

        <div class="HGright">
        </div>
    </div>
</body>
</html>